-- 코드를 입력하세요
SELECT BOOK_ID, AUTHOR_NAME, DATE_FORMAT(PUBLISHED_DATE, "%Y-%m-%d") AS PUBLISHED_DATE FROM BOOK AS A JOIN AUTHOR AS B ON A.author_id=B.author_id WHERE category="경제" ORDER BY PUBLISHED_DATE;